name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  - wheel
  - pytest-pep8
  - build

  # Doc
  - sphinx
  - sphinx_rtd_theme

    # Pip-only installs
  #- pip:
  #  - codecov

